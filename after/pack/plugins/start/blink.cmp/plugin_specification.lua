require('packer').use({ 'saghen/blink.cmp' })

-- Dependencies
require('packer').use({ 'becknik/blink-cmp-luasnip-choice' })
require('packer').use({ 'mayromr/blink-cmp-dap' })
require('packer').use({ 'Kaiser-Yang/blink-cmp-git' })
require('packer').use({ 'kyazdani42/nvim-web-devicons', opt = true })
